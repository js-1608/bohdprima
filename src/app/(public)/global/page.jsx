import Global from "../../../views/Global";

export const metadata = {
  title: "Global Network | Bodh Prima",
  description: "Bodh Prima operates across a vast global network. See our major ports, shipping routes, and global trade destinations.",
  keywords: "global shipping network, trade routes, international ports, export destinations, import logistics",
};

export default function Page() {
  return <Global />;
}
